#include <cmath>
#include <vector>
#include <Rcpp.h>

using namespace std;

bool threeway(vector < long > xx,vector < long > yy, vector < long > zz){
  vector< int > xyz;
  xyz.reserve(x.size() + B.size()+z.size());
  xyz.insert(xyz.end(), xx.begin(), xx.end());
  xyz.insert(xyz.end(), yy.begin(), yy.end());
  xyz.insert(xyz.end(), zz.begin(), zz.end());
  sort(xyz.begin(),xyz.end())
}